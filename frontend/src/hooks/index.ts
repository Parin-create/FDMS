/**
 * App-wide reusable hooks (business-agnostic).
 *
 * Cross-cutting hooks shared by multiple feature modules — e.g. useDebounce,
 * useMediaQuery, usePagination. Feature-specific hooks live under
 * "src/features/<module>/hooks" instead.
 *
 * No hooks yet (Sprint 4.1 scaffolding). Re-export each hook here as it is added.
 */
export {};
