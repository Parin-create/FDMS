/**
 * App-wide shared TypeScript types.
 *
 * Cross-cutting types shared across feature modules — e.g. pagination envelopes,
 * common API shapes. Prefer inferring types from zod schemas (z.infer) where a
 * runtime schema exists. Feature-specific types live under
 * "src/features/<module>/types" instead.
 *
 * No types yet (Sprint 4.1 scaffolding). Re-export shared types here as added.
 */
export {};
