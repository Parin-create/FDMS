/**
 * App-wide pure utility functions (business-agnostic, side-effect free).
 *
 * Shared helpers used across feature modules — e.g. formatBytes, formatDate,
 * classnames helpers. Keep these pure and unit-testable.
 *
 * No utilities yet (Sprint 4.1 scaffolding). Re-export each helper here as added.
 */
export {};
