/**
 * Shared, presentational UI primitives (design-system layer).
 *
 * Reusable, business-agnostic components shared across all feature modules —
 * e.g. Button, IconButton, Modal/Dialog, Table, Toast (+ provider), ProgressBar,
 * Spinner, EmptyState, Menu/Dropdown, Dropzone base. Built with Tailwind.
 *
 * No components yet (Sprint 4.1 scaffolding). Re-export each primitive here as it
 * is added so features can import from "@/components/ui".
 */
export {};
